feat(api): Implement GET /api/events/[eventId] to fetch event details and RSVPs

- Implemented the Next.js Route Handler for `GET /api/events/[eventId]`.
- Fetches event details from the Turso `events` table.
- Fetches all associated RSVPs from the `rsvps` table.
- Handles JSON parsing for `time_slots` and `selected_slots`.
- Returns combined event and RSVP data.
- Includes 404 error handling for non-existent events.