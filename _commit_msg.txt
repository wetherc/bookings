feat(api): Implement POST /api/rsvps to submit/update RSVPs

- Implemented the Next.js Route Handler for `POST /api/rsvps`.
- Generates `respondent_token` if not provided.
- Uses `INSERT OR REPLACE` for upsert functionality in the `rsvps` table.
- Stores `selected_slots` as a JSON string.
- Returns the `respondent_token` for client-side tracking.