feat(api): Implement PUT /api/events/[eventId] to update event details

- Added `PUT` handler to `src/app/api/events/[eventId]/route.ts`.
- Verifies `admin_token` for authorization.
- Dynamically constructs and executes SQL `UPDATE` statements for event details.
- Handles authorization failures and non-existent events.