feat(api): Implement POST /api/events to create new events

- Implemented the Next.js Route Handler for `POST /api/events`.
- Generates unique `event_id` and `admin_token` using `nanoid`.
- Inserts event details into the Turso `events` table.
- Stores `time_slots` as a JSON string.
- Returns `event_id` and `admin_token` upon successful creation.