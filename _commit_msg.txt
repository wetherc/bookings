feat(frontend): Implement admin view page for events

- Created `src/app/events/[eventId]/admin/page.tsx`.
- Fetches event details and all RSVPs using `GET /api/events/[eventId]`.
- Extracts `admin_token` from URL for authorization.
- Displays a shareable public link with copy functionality.
- Provides an editable form for event title, description, block minutes, and time slots.
- Handles event updates via `PUT /api/events/[eventId]`.
- Visualizes RSVP summary by time slot, showing attendees for each.