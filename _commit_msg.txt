feat(frontend): Implement public RSVP page for events

- Created `src/app/events/[eventId]/page.tsx` to display event details and allow RSVPs.
- Fetches event and RSVP data using `GET /api/events/[eventId]`.
- Implements UI for name input and time slot selection.
- Stores/retrieves `respondent_token` in `localStorage` for returning users.
- Submits RSVP data via `POST /api/rsvps` endpoint.
- Displays basic RSVP list and loading/error states.