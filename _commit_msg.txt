feat(frontend): Implement event creation form on homepage

- Replaced default content of `src/app/page.tsx` with an event creation form.
- Form includes fields for title, description, block duration, and time slots.
- Uses `useState` for form state and `useRouter` for navigation.
- Handles form submission by calling `POST /api/events` endpoint.
- Redirects to the admin page upon successful event creation.